# Public Google Analytics 4 measurement ID (safe to commit — ships in the client bundle).
# Loaded by `next build` (production). Dev runs without analytics.
NEXT_PUBLIC_GA_ID=G-EFZYCSLESW

# Sanity (public read dataset — no token needed). Used to fetch products at build time.
NEXT_PUBLIC_SANITY_PROJECT_ID=p4xuisrx
NEXT_PUBLIC_SANITY_DATASET=production
